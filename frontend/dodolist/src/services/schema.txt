-- Schema

table: task_lists
Optional id
	String 	Plain text value. It is autogenerated if not set.
Required user_id
	String 	Relation record id.
Required name
	String 	Plain text value.
Optional color
	String 	Plain text value.
Required createdAt
	String 	
Optional pinned
	Boolean 	
Optional archived
	Boolean 	
Optional yjsUpdate
	String 	Plain text value.
Optional deleted
	Boolean

const data = {
    "user_id": "RELATION_RECORD_ID",
    "name": "test",
    "color": "test",
    "createdAt": "2022-01-01 10:00:00.123Z",
    "pinned": true,
    "archived": true,
    "yjsUpdate": "test",
    "deleted": true
};

const record = await pb.collection('task_lists').create(data);

// example update data
const data = {
    "user_id": "RELATION_RECORD_ID",
    "name": "test",
    "color": "test",
    "createdAt": "2022-01-01 10:00:00.123Z",
    "pinned": true,
    "archived": true,
    "yjsUpdate": "test",
    "deleted": true
};

const record = await pb.collection('task_lists').update('RECORD_ID', data);

// Subscribe to changes in any task_lists record
pb.collection('task_lists').subscribe('*', function (e) {
    console.log(e.action);
    console.log(e.record);
}, { /* other options like: filter, expand, custom headers, etc. */ });

// Subscribe to changes only in the specified record
pb.collection('task_lists').subscribe('RECORD_ID', function (e) {
    console.log(e.action);
    console.log(e.record);
}, { /* other options like: filter, expand, custom headers, etc. */ });

// Unsubscribe
pb.collection('task_lists').unsubscribe('RECORD_ID'); // remove all 'RECORD_ID' subscriptions
pb.collection('task_lists').unsubscribe('*'); // remove all '*' topic subscriptions
pb.collection('task_lists').unsubscribe(); // remove all subscriptions in the collection