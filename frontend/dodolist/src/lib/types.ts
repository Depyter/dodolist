export interface Todo {
  id: string;
  text: string;
  description?: string;
  completed: boolean;
  createdAt: Date;
  completedAt?: Date;
  deadline?: Date;
  reminder?: Date;
  recurring?: "none" | "daily" | "weekly" | "monthly";
  listId: string;
}

export interface UserProfile {
  id: string;
  email: string;
  username: string;
  name?: string;
  verified: boolean;
  avatar?: string;
}

export interface PocketBaseTaskListRecord {
  id?: string; // Optional, autogenerated if not set
  user_id: string; // Required, relation record id
  createdAt: string; // Required
  yjsUpdate?: string; // Optional, plain text value
  yjsClientId?: string; // Optional, plain text value
  shared_with?: Array<string>
  collaborators?: Array<string>
}

export interface TodoList {
  id: string;
  user_id: string;
  name: string;
  color: string;
  createdAt: string;
  pinned?: boolean;
  archived?: boolean;
  yjsUpdate?: string;
  deleted?: boolean;
  shared_with?: string[];
  collaborators?: string[];
  readOnly?: boolean;
}

export interface TodoListWithTodos extends TodoList {
  todos: Todo[];
}